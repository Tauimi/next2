# 🚀 Быстрое решение проблемы с авторизацией на Vercel

## Шаг 1: Диагностика (2 минуты)

### 1.1 Откройте диагностический endpoint
Перейдите по ссылке: `https://ваш-проект.vercel.app/api/debug`

Вы увидите детальную информацию о состоянии системы.

### 1.2 Проверьте здоровье системы
Откройте: `https://ваш-проект.vercel.app/api/health`

**Ожидаемый результат:**
```json
{
  "status": "healthy",
  "database": {
    "status": "healthy",
    "latency": 45
  }
}
```

## Шаг 2: Исправление (5 минут)

### Проблема 1: ❌ NEXTAUTH_SECRET отсутствует

**Решение:**

1. **Сгенерируйте секретный ключ:**
   ```bash
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```
   
   Или используйте онлайн генератор: https://generate-secret.vercel.app/32

2. **Добавьте в Vercel:**
   - Откройте: https://vercel.com/dashboard
   - Выберите ваш проект
   - Settings → Environment Variables
   - Нажмите "Add New"
   - Name: `NEXTAUTH_SECRET`
   - Value: ваш_сгенерированный_ключ (минимум 32 символа)
   - Выберите все окружения: Production, Preview, Development
   - Нажмите "Save"

3. **Redeploy проекта:**
   - Deployments → последний деплой → три точки → Redeploy
   - Или сделайте новый commit в Git

### Проблема 2: ❌ NEXTAUTH_URL неправильный

**Решение:**

1. **Узнайте ваш домен Vercel:**
   - Vercel Dashboard → ваш проект → Domains
   - Скопируйте основной домен (например: `your-project.vercel.app`)

2. **Добавьте/обновите в Vercel:**
   - Settings → Environment Variables
   - Name: `NEXTAUTH_URL`
   - Value: `https://your-project.vercel.app` (БЕЗ слэша в конце!)
   - Сохраните

3. **Redeploy проекта**

### Проблема 3: ❌ База данных недоступна

**Решение A: Создать Vercel Postgres (рекомендуется)**

1. **Создайте базу данных:**
   - Vercel Dashboard → Storage
   - Create Database → Postgres
   - Выберите регион (ближайший к вам)
   - Создайте базу

2. **Скопируйте переменные:**
   - Откройте созданную БД
   - Вкладка `.env.local`
   - Скопируйте все переменные

3. **Добавьте в Environment Variables:**
   - Вернитесь в Settings → Environment Variables
   - Добавьте:
     - `DATABASE_URL` = значение из `POSTGRES_PRISMA_URL`
     - `DATABASE_URL_NON_POOLING` = значение из `POSTGRES_URL_NON_POOLING`

4. **Примените схему БД:**
   ```bash
   # Установите Vercel CLI
   npm i -g vercel
   
   # Войдите
   vercel login
   
   # Подключитесь к проекту
   vercel link
   
   # Загрузите переменные окружения
   vercel env pull .env.local
   
   # Примените схему
   npx prisma db push
   ```

5. **Redeploy проекта**

**Решение B: Использовать внешнюю БД (Supabase, Neon и т.д.)**

1. **Создайте PostgreSQL базу** у любого провайдера
2. **Получите строку подключения** (Connection String)
3. **Добавьте в Vercel:**
   - `DATABASE_URL` = ваша строка подключения
4. **Примените схему** (см. выше)
5. **Redeploy**

### Проблема 4: ❌ Схема БД не применена

**Решение:**

```bash
# Способ 1: Через Vercel CLI (рекомендуется)
vercel env pull .env.local
npx prisma db push

# Способ 2: Через локальную БД
# Если у вас есть доступ к SQL консоли вашей БД,
# выполните SQL команды из prisma/schema.prisma
```

## Шаг 3: Проверка (1 минута)

### 3.1 Проверьте диагностику снова
Откройте: `https://ваш-проект.vercel.app/api/debug`

**Все должно быть зеленым (✅):**
- ✅ NEXTAUTH_SECRET установлен
- ✅ NEXTAUTH_URL правильный
- ✅ DATABASE_URL установлен
- ✅ База данных подключена
- ✅ Таблицы существуют
- ✅ JWT работает

### 3.2 Попробуйте зарегистрироваться
1. Откройте ваш сайт
2. Перейдите на страницу регистрации
3. Зарегистрируйте нового пользователя
4. **Первый пользователь автоматически станет администратором!**

### 3.3 Проверьте админ-панель
После регистрации перейдите на: `https://ваш-проект.vercel.app/admin`

## 🎯 Чеклист успешного деплоя

Отметьте каждый пункт:

- [ ] `NEXTAUTH_SECRET` добавлен в Vercel (минимум 32 символа)
- [ ] `NEXTAUTH_URL` соответствует домену Vercel
- [ ] `DATABASE_URL` добавлен и база доступна
- [ ] Схема Prisma применена (`npx prisma db push`)
- [ ] Проект переdeployен после добавления переменных
- [ ] `/api/debug` показывает все зеленые галочки
- [ ] `/api/health` возвращает "healthy"
- [ ] Регистрация работает
- [ ] Первый пользователь получил права админа

## 🆘 Если проблема не решилась

### Посмотрите логи Vercel:
1. Vercel Dashboard → ваш проект
2. Deployments → последний деплой
3. Functions → найдите `/api/auth/register` или `/api/auth/login`
4. Посмотрите ошибки в логах

### Типичные ошибки в логах:

**"NEXTAUTH_SECRET is required"**
→ Добавьте NEXTAUTH_SECRET в Environment Variables

**"PrismaClientInitializationError"**
→ Проблема с DATABASE_URL или база недоступна

**"Invalid `prisma.user.create()` invocation"**
→ Схема БД не применена, выполните `npx prisma db push`

**"P2002: Unique constraint failed"**
→ Пользователь с таким email уже существует

## 📞 Дополнительная помощь

Если ничего не помогло:

1. **Проверьте `/api/debug`** - он покажет точную проблему
2. **Посмотрите логи Vercel** - там будет детальная ошибка
3. **Проверьте статус вашей БД** - возможно она недоступна
4. **Попробуйте локально** - `npm run build && npm start`

## 💡 Полезные ссылки

- Vercel Dashboard: https://vercel.com/dashboard
- Vercel Docs: https://vercel.com/docs
- Prisma Docs: https://www.prisma.io/docs
- Генератор секретов: https://generate-secret.vercel.app/32

---

**Время решения: ~10 минут**

После выполнения всех шагов ваша авторизация должна работать! 🎉
